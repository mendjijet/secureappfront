services:
  secureappfront:
    container_name: secureappfrontcontainer
    build: .
    image: secureappfront:v1
    ports:
      - 80:80
    expose:
      - 80
    networks:
      - internalnet

networks:
  internalnet:
    driver: bridge
